# 작업 규칙

- 프로젝트 역할과 설계 기준은 `skill.md`를 따른다. 사용자의 명시적 요청을 우선한다.
- 요청과 직접 관련된 파일·심볼부터 검색하고 필요한 구간만 읽는다.
- 기존 구현을 재사용하고 최소 범위만 변경한다. 불필요한 추상화·의존성·리팩터링은 추가하지 않는다.
- 이미 확인한 내용은 이유 없이 반복해서 읽지 않는다.
- 작은 작업에는 sub-agent나 광범위한 병렬 탐색을 사용하지 않는다.
- 모호함이 결과에 중대한 영향을 주지 않으면 합리적으로 판단한다. 데이터 손실·보안·API 계약·DB 변경처럼 영향이 크면 확인한다.
- 변경과 직접 관련된 좁은 검증부터 수행하고 실패하거나 필요할 때만 범위를 넓힌다.
- 장시간 작업은 현재 목표·결정·변경 파일·남은 작업·검증 상태 중심으로 정리한다.
- 토큰 절감을 위해 정확성·보안·필요한 검증을 생략하지 않는다.
- 비밀값, 모델, 생성 음성, 실제 방송·시청자 기록을 커밋하지 않는다.
- 외부 엔진은 별도 실행한다. 통신 모듈과 프롬프트 구성 및 실행 흐름의 책임을 분리한다.
- 의존성은 pyproject.toml과 uv.lock으로 관리하며 환경과 CI에 --locked를 적용한다.
- 완료 시 변경 사항과 검증 결과만 간결하게 보고하고 관련 없는 추가 개선은 하지 않는다.
