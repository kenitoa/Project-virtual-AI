# 초기 구성

터미널 → 제한된 입력 대기열 → 캐릭터 프롬프트 → 교체 가능한 LLMClient → mock 응답 → 출력 검사 → 텍스트 출력.
원시 응답·최종 답변·음성용 문자열을 분리하며 외부 음성 어댑터는 연결하지 않습니다.
최근 기록은 아직 저장하지 않습니다. 캐릭터 설정만 메시지에 포함됩니다.
일반 입력은 데이터로만 취급하고 셸이나 외부 작업으로 실행하지 않습니다.
대기열 크기와 유효시간, 입력·출력 길이는 설정으로 제한합니다.
로그는 원문이나 사용자 식별자 없이 응답 ID·대기·생성 시간과 상태만 기록합니다.
