# 2026-09-21 작업 일지

오늘 저장소의 Git 커밋과 프로젝트 문서에 기록된 작업·검증 결과를 정리했다. 아래 테스트 결과는 기존 검증 기록이며, 이 일지 작성 중 다시 실행한 결과는 아니다.

## 1. 프로젝트 기반 구성

- Python 제어 프로그램의 초기 구조와 mock 실행 모드, 설정 로더, 실행 진입점, 자동 테스트 및 CI를 구성했다.
- 의존성을 `pyproject.toml`과 `uv.lock`으로 관리하고 `--locked` 기반 설치·검증 흐름을 마련했다.
- `skill.md`에 저장소 역할, 외부 엔진과의 책임 분리, 단계별 개발 범위와 완료 기준을 정리했다.
- 외부 엔진·모델·비밀값·실제 대화 및 생성 음성을 저장소에서 제외하는 기준을 마련했다.

## 2. KoboldCpp 텍스트 대화 연결

- KoboldCpp HTTP 클라이언트를 구현하고 응답 검사, 제한된 재시도, 단계별·전체 시간 제한 및 오류 처리를 추가했다.
- 캐릭터 설정과 시스템 프롬프트를 분리하고 최근 대화 기록을 프롬프트에 반영했다.
- 사용자별 기록 분리, 턴 수·문자 수·보관 시간·사용자 수 제한과 컨텍스트 예산을 적용했다.
- `/stop`으로 진행 중 응답과 대기 입력을 취소하고, `/forget`으로 기록을 삭제하며, `/quit`으로 자원을 정리하도록 구성했다.
- Windows 콘솔 출력 문제를 수정하고 실제 KoboldCpp 1.121 / Qwen3-8B Q4_K_M 연결을 검증했다.
- 실제 한국어 답변 출력, CLI 정상 종료, 서버 연결 실패 후 재시작 복구를 확인했다.

## 3. GPT-SoVITS 음성 합성

- 별도로 실행하는 GPT-SoVITS `/tts` API 클라이언트와 고정 문장 합성 CLI를 구현했다.
- WAV 형식·크기 검증, 시간 제한, 안전한 파일 저장, 실패·취소 시 기존 파일 보존을 적용했다.
- 실제 GPT-SoVITS v2 환경에서 한국어 테스트 문장의 WAV 저장과 서버 재시작 후 복구를 확인했다.
- 엔진 환경은 프로젝트 환경과 분리했다. KSS 참조 음성은 비상업적 로컬 검증에 사용했으며 방송용 사용 허가로 간주하지 않는다.

## 4. WAV 재생 및 대화 음성 연결

- 출력 장치 조회·선택, WAV 재생, 재생 중지, 장치 자원 해제 기능과 독립 오디오 CLI를 구현했다.
- LLM 답변의 텍스트 표시 후 정리된 `speech`를 합성·재생하도록 연결했다.
- LLM 생성부터 재생 종료까지 응답을 한 건씩 처리하고, 중지·실패·종료 시 임시 WAV를 정리하도록 했다.
- TTS나 오디오 오류가 나도 텍스트 대화를 계속할 수 있도록 처리했다.
- 실제 출력 스트림의 재생·중단·해제, 중지 후 다음 질문 처리, 임시 파일 삭제를 확인했다.
- 통합 재검증 기록: LLM 약 2.402초, TTS 약 3.000초, 재생·해제 약 4.815초. 이는 해당 실행의 관측값이다.

## 5. KoboldCpp 서버 생성 중단 보강

- 요청마다 고유 `genkey`를 부여하고 해당 요청만 서버에서 중단하도록 구현했다.
- 중단 응답뿐 아니라 서버의 `idle: 1, queue: 0`을 확인한 뒤 다음 생성을 허용하도록 했다.
- 정리 실패나 상태 불명 시 이후 생성을 차단하여 이전 추론과 새 요청이 겹치는 상황을 방지했다.
- 설정은 기본 비활성화이며 검증한 서버 계약에 맞춰 활성화하도록 문서화했다.
- 실제 생성 중 중단 → 이전 답변 출력·기록 차단 → 같은 앱에서 다음 질문 성공을 3회 확인했다. 서버 정리는 약 0.305~0.378초였다.

## 검증 상태

| 범위 | 오늘 기록된 결과 |
| --- | --- |
| 음성 통합 단계 | 전체 테스트 174개, Ruff 및 포맷 검사 통과 |
| 서버 중단 보강 후 | 대상 테스트 73개, 전체 pytest 204개, Ruff lint·format 검사 통과 |
| 실제 LLM | 답변 생성, 오류 복구, 요청별 중단 후 재개 확인 |
| 실제 TTS·오디오 | WAV 생성, 출력 스트림 처리, 중지·자원 해제 및 파일 정리 확인 |
| 사람의 직접 청취 | 미확인. 발음·잡음·잘림과 중지 후 무음은 추가 확인 필요 |

## 남은 작업

- 실제 스피커·헤드폰으로 일반 답변 음질, 재생 중 `/stop`과 다음 질문, 합성 중 `/stop`의 늦은 재생 방지, 재생 중 `/quit`과 재실행을 확인한다.
- 음성 통합은 직접 청취 완료 전 Draft 유지·병합 보류라는 기록을 따른다. 현재 원격 PR 상태는 이 일지 작성에서 새로 조회하지 않았다.
- 음성 통합 병합 후 서버 중단 수정 브랜치의 기준과 PR 대상을 정리한다.
- 이후 VTube Studio 연결·인증·허용 표정, 입 움직임, OBS 로컬 녹화를 순서대로 진행한다. 방송 채팅과 장기 기억은 후속 범위다.

## 근거

- 오늘의 주요 커밋: `8afa5c5` 초기 구성, `e99c3e3` KoboldCpp 연결, `36b0fe5` 취소 처리, `6f264bb` 기록·프롬프트 정책, `d9d1f07` Windows 출력·실제 연결 검증, `de102bc` TTS 클라이언트, `5bc00ad` 오디오·음성 통합, `92061b0` 청취 검증 기준, `13fb4b1` 서버 생성 중단.
- 상세 기록: [README](README.md), [백엔드 검증](docs/backends.md), [오디오 검증](docs/audio.md), [음성 파이프라인](docs/voice-pipeline.md).
- 일부 이전 문서에는 서버 중단이 미구현으로 남아 있다. 서버 중단의 최신 상태는 `13fb4b1`과 `docs/backends.md`의 요청별 서버 중단 기록을 기준으로 정리했다.
